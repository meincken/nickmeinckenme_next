import { SkillsList } from "@/clientComponents";

export const Skills = () => {
  return (
    <div id="skills" className="grid_container mx-auto">
      <h2>Skills</h2>
      <SkillsList />
    </div>
  );
}
