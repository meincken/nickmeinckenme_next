import { Nav } from "@/clientComponents/"

export const Navigation = () => {
  return (
    <nav className="sticky top-0 bg-black text-white flex content-center justify-center">
      <Nav />
    </nav>
  )
}
