import { Nav } from "@/clientComponents/"

export const Navigation = () => {
  return (
    <nav className="fixed w-full top-0 bg-black text-white flex content-center justify-center z-10">
      <Nav />
    </nav>
  )
}
